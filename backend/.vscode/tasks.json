{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "build and run backend",
        "type": "shell",
        "command": "cmake --build build && ./build/MyDSPApp",
        "group": {
          "kind": "build",
          "isDefault": true
        },
        "problemMatcher": [],
        "options": {
          "cwd": "${workspaceFolder}/"
        }
      }
    ]
}
